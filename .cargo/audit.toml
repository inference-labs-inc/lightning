[advisories]
ignore = [
    # wasmtime 8.0.1 — lockfile phantom from subxt -> polkadot-sdk -> sc-executor-wasmtime; never compiled (subtensor-tests feature off by default). Remove when subxt no longer pulls polkadot-sdk.
    "RUSTSEC-2023-0091",
    "RUSTSEC-2024-0438",
    "RUSTSEC-2025-0118",

    # wasmtime / wasmtime-jit-debug 8.0.1 — same lockfile phantom chain as above.
    "RUSTSEC-2024-0442",
    "RUSTSEC-2026-0020",
    "RUSTSEC-2026-0021",

    # proc-macro-error 1.0.4 — unmaintained. Lockfile phantom from subxt -> polkadot-sdk -> aquamarine/alloy-sol-macro. Not compiled by btlightning.
    "RUSTSEC-2024-0370",

    # sodiumoxide 0.2.7 — deprecated but actively compiled via btwallet (not a lockfile phantom). Remove when btwallet migrates to a maintained crypto library.
    "RUSTSEC-2021-0137",

    # lru 0.8.1/0.12.5 — unsound IterMut. Lockfile phantoms from polkadot-sdk (parity-util-mem) and subxt (smoldot-light). Not compiled by btlightning.
    "RUSTSEC-2026-0002",
]

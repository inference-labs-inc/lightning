from setuptools import setup, find_packages

setup(
    name="lightning",
    version="0.0.1",
    author="Inference Labs Inc",
    description="Python bindings for Lightning QUIC Client",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[],
)
